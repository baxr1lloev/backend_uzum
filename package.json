{
  "name": "uzum-back",
  "version": "1.0.0",
  "description": "This is Fake Rest Api (for testing)",
  "main": "index.js",
  "scripts": {
    "json-server": "json-server --watch db.json -p 3000",
    "start": "json-server -p 3000 -w db.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  }
}
